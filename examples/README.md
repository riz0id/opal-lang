# Examples

Small Opal source files that exercise specific language features. Each
example is self-contained and pairs with a comment header at the top of
the source file describing what it demonstrates.

| File | Demonstrates |
|---|---|
| `macro-hygiene.opal` | Use-site and inside-edge scopes (the scope-sets paper §4.2). The canonical test case for the macro-state refactor in `plans/expander-macro-state-refactor.md`. |
